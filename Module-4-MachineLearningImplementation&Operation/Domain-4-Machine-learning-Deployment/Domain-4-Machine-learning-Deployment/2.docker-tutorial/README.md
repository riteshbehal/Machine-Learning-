# Docker Commands
```
docker pull busybox

docker run busybox

docker run busybox echo "hello from busybox"

docker ps

docker ps -a

docker run -it busybox sh
exit()

docker build murthy/myimage .

docker run -p 5000:5000 murthy/myimage

```